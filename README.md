Dell of Death is a Windows driver utility,
optimized for Dell PC's. It finds drivers using
your Dell's model number, service tag and system ID, (SKU). 
Close the installer before running the app.
It needs to be run as admin.
